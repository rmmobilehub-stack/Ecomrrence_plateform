'use client';
import {useEffect,useState} from 'react';
type O={id:string;orderNumber:string;customer:{name:string;email:string};total:number;status:string;createdAt:string};
export default function GlobalOrdersPage(){const [orders,setOrders]=useState<O[]>([]);useEffect(()=>{fetch('/api/super-admin/orders').then(r=>r.json()).then(d=>setOrders(d.orders??[]))},[]);return <><div className="page-header"><div><h1 className="page-title">All orders</h1><p className="page-subtitle">Orders across every active store.</p></div></div><div className="glass-card table-container"><table className="data-table"><thead><tr><th>Order</th><th>Customer</th><th>Total</th><th>Status</th><th>Date</th></tr></thead><tbody>{orders.length===0?<tr><td colSpan={5}>No orders yet.</td></tr>:orders.map(o=><tr key={o.id}><td>{o.orderNumber}</td><td>{o.customer.name}<small>{o.customer.email}</small></td><td>${o.total.toFixed(2)}</td><td><span className="badge badge-warning">{o.status}</span></td><td>{new Date(o.createdAt).toLocaleDateString()}</td></tr>)}</tbody></table></div></>}
