'use client';
import { useEffect, useMemo, useState } from 'react';
import dynamic from 'next/dynamic';
import { formatMoney } from '@/lib/currency';
import { useAdminStore } from '@/components/admin/useAdminStore';
const RevenueBarChart = dynamic(() => import('@/components/charts/RevenueBarChart'), { ssr: false, loading: () => <div className="chart-loading" /> });
type Order = { createdAt: string; total: number; status: string };
export default function AnalyticsPage() { const store = useAdminStore(); const currency = store?.currency ?? 'PKR'; const [orders, setOrders] = useState<Order[]>([]); useEffect(() => { fetch('/api/admin/orders').then(response => response.json()).then(data => setOrders(data.orders ?? [])); }, []); const points = useMemo(() => Array.from({ length: 7 }, (_, index) => { const date = new Date(); date.setDate(date.getDate() - (6 - index)); return { day: date.toLocaleDateString(undefined, { weekday: 'short' }), revenue: orders.filter(order => new Date(order.createdAt).toDateString() === date.toDateString() && order.status !== 'cancelled').reduce((sum, order) => sum + order.total, 0) }; }), [orders]); const total = orders.filter(order => order.status !== 'cancelled').reduce((sum, order) => sum + order.total, 0); return <><div className="page-header"><div><h1 className="page-title">{store?.name ?? 'Store'} analytics</h1><p className="page-subtitle">Revenue performance for this store only.</p></div><div className="badge badge-success">{formatMoney(total, currency)} all-time revenue</div></div><div className="glass-card chart-card"><div className="chart-title">Daily revenue</div><RevenueBarChart data={points} currency={currency}/></div></>; }
