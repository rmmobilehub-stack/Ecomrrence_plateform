'use client';
import { createContext,useContext,useEffect,useMemo,useState } from 'react';
import type { CartItem } from '@/lib/types';

type CartContextValue={items:CartItem[];add:(item:CartItem)=>void;update:(index:number,qty:number)=>void;remove:(index:number)=>void;clear:()=>void;count:number;subtotal:number;currency:string};
const CartContext=createContext<CartContextValue|null>(null);
export function CartProvider({slug,currency,children}:{slug:string;currency:string;children:React.ReactNode}){const key=`shopsaas-cart-${slug}`;const [items,setItems]=useState<CartItem[]>([]);useEffect(()=>{try{setItems(JSON.parse(localStorage.getItem(key)??'[]'))}catch{}},[key]);useEffect(()=>{localStorage.setItem(key,JSON.stringify(items))},[items,key]);const value=useMemo(()=>({items,add:(item:CartItem)=>setItems(current=>{const index=current.findIndex(x=>x.productId===item.productId&&JSON.stringify(x.selectedVariants)===JSON.stringify(item.selectedVariants));return index<0?[...current,item]:current.map((x,n)=>n===index?{...x,qty:x.qty+item.qty}:x)}),update:(index:number,qty:number)=>setItems(current=>qty<1?current.filter((_,n)=>n!==index):current.map((x,n)=>n===index?{...x,qty}:x)),remove:(index:number)=>setItems(current=>current.filter((_,n)=>n!==index)),clear:()=>setItems([]),count:items.reduce((s,x)=>s+x.qty,0),subtotal:items.reduce((s,x)=>s+x.qty*x.price,0),currency}),[items,currency]);return <CartContext.Provider value={value}>{children}</CartContext.Provider>}
export function useCart(){const context=useContext(CartContext);if(!context)throw new Error('useCart must be used inside CartProvider');return context}
